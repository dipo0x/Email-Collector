extends ../layout

block content

  form(action="/leads/" + id + "/edit/" method="POST") 
    input(name="lead_email", type="email", placeholder="Enter your email bruh", value=email)
    button(type="submit") Save edit
  
  if title 
    h1 title